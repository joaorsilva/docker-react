language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t joaorsilva/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true joaorsilva/docker-react npm run test

deploy:
  provider: elasticbeanstalk
  region: "us-east-2"
  app: "docker"
  env: "Docker-env"
  bucket_name: "https://s3.console.aws.amazon.com/s3/buckets/elasticbeanstalk-us-east-2-965749552422/?region=us-east-2&tab=overview"
  bucket_path: "docker"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: "$AWS_SECRET_KEY"
